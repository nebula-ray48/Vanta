### 🎨 1. アート表現・絵作りについての考察

#### ① 「PBR環境 × セルルックキャラ」が喧嘩しないためのキモ
PBR の世界にそのままキャラを置くと、「顔にポリゴンや鼻のリアルすぎる影が落ちて汚くなる」「環境光（IBL）の照り返しでアニメ塗りの色が濁る」という問題が起きます。
ここを解消するための**独自アルゴリズムの引き出し**としては以下が定番かつ強力です：
- **スタイライズド・ディフューズ（階調・ランプ）**: $N \cdot L$ の生の値ではなく、なだらかな多段ステップやグラデーション（Ramp）で影を落とす。
- **顔専用の影制御（Face Shadow Map / Sdf / 頂点カラー法線）**: 顔の凸凹に影響されず、光の角度に応じて綺麗なアニメ影を落とす。
- **IBL（環境光）の彩度・コントラスト制御**: PBR オブジェクトにはフルで IBL を効かせ、キャラには「濁らない程度に淡く色味だけ乗せる」ブレンド。

#### ② 「アナモフィック・シネマティック」ポストプロセス
アナモフィックレンズ特有のルックを作るために、ポストプロセスパイプラインに以下を組み込むと一気に映画的なルックになります：
- **横方向のブルーム / レンズフレア（Streak Blur）**: 水平方向に引き伸ばされた光の筋。
- **楕円ボケ（DOF） & 周辺減光（Vignette） & わずかな色収差（Chromatic Aberration）**
- **ACES トーンマッピング + カラーグレーディング（LUT）**: これにより、PBR 背景と Toon キャラの色調が1つのシネマティックなトーンに綺麗にまとまります。

---

### 🏗️ 2. `MaterialType enum` 切り替えの設計レビュー

あなたが提案してくれた **「`MaterialType enum` を持たせて切り替える」** というアイデアは、まさに正攻法です。

これを Vulkan / GPU 側でどう落とし込むかには、大きく分けて **2つのアプローチ** があります。

```text
【アプローチ A: パイプライン完全分離（おすすめ ⭐⭐⭐）】
  1. C++ の Mesh/Material に `MaterialType` (PBR, Toon, Skybox 等) を持たせる。
  2. 描画ループで MaterialType ごとに描画コマンド（Indirect Draw）をグループ分けする。
  3. PBR パイプラインをバインドして PBR 群を一括描画
     ➔ Toon パイプラインをバインドして Toon 群を一括描画。

【アプローチ B: ウーバーシェーダー（1つのシェーダー内で分岐）】
  1. 頂点/フラグメントシェーダー内で `if (material.type == TOON)` のように動的分岐する。
```

#### なぜ「アプローチ A（パイプライン分離）」が良いか？
1. **シェーダーコードが圧倒的に綺麗・軽量になる**:
    - PBR シェーダーは物理計算（GGX / Disney PBR）に特化できる。
    - Toon シェーダーは独自のセル計算・輪郭線・リムライトに特化できる。
2. **GPU の実行効率が良い**:
    - 1つのシェーダーに全コードを詰め込むと、レジスタ使用量が増えて GPU の並列処理性能が落ちます。
3. **Vanta の現在のアーキテクチャ（Bindless）と相性抜群**:
    - Descriptor Set 0（テクスチャやバッファ）は全パイプライン共通のまま、シェーダー（Pipeline）だけを1回切り替えるだけで済むため、切り替えオーバーヘッドが極小です。

---

### 💡 3. データ構造のイメージ（叩き台）

例えば、C++ 側とシェーダー側で以下のようなイメージを持つと自然です：

```cpp
// C++ 側のマテリアル定義
enum class MaterialType : uint32_t {
    PBR = 0,
    Toon = 1,
    // 将来的に Hair, Eye, Skin などを追加可能
};

struct MaterialData {
    MaterialType type = MaterialType::PBR;
    uint32_t baseColorTexIndex;
    uint32_t normalTexIndex;
    // PBR用
    float roughness;
    float metallic;
    // Toon用
    float shadowThreshold; // 影の境界の閾値
    float shadowFeather;   // 影のボケ足
    // ...
};
```

---

### 🎯 次のステップ：どこから手をつけますか？

あなたが自力で組み立てていくにあたり、最初の足がかりとしてどちらから進めたいですか？

1. **【シェーダー側から攻める】**:  
   まず `assets/shaders/` に `toon_main.slang` を新設し、セルルック（ランプ陰影やリムライト）のライティング計算式を書いてみる。
2. **【C++ / パイプライン側から攻める】**:  
   C++ 側の `MaterialType` や `PbrPipeline` / `ToonPipeline` のクラス・データ構造を整理して、パイプラインを2つ生成できるようにする。

あなたのやりやすい方からスタートしましょう！どう進めたいか教えてください。
